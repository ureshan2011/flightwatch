# Handoff prompt — ship the redesign + real Firebase

> Paste everything in the fenced block below to the implementing agent/developer.
> It is self-contained context for converting the prototype into the live site and
> wiring real Firebase, without touching the scraper, the CSV dataset, or the model.

---

````text
You are implementing a redesign for FlightWatch ("Faro"), a free, self-hosted
"should I book this flight now or wait?" fare tracker. Your job: replace the current
generated dashboard with the new three-view app that already exists as a prototype,
and wire REAL Firebase — while keeping the existing scraper → CSV pipeline and the
forecasting model completely intact and NOT destroying any committed data.

═══════════════════════════════════════════════════════════════════════════════
READ THESE FIRST (do not write code until you have)
═══════════════════════════════════════════════════════════════════════════════
• README.md                      — what the product is, the pipeline, the CLI.
• flightwatch/dashboard.py       — the generator you are rewriting. ~2600 lines; its
                                   _html() builds docs/index.html, inlines CSS/JS, and
                                   embeds the data as `const D = {...}` (payload) plus
                                   writes docs/explore.json (~4.3MB finder grid).
• flightwatch/__init__.py, collect.py, storage.py, predict.py, analytics.py,
  alerts.py, __main__.py          — the data/model/CLI layers. DO NOT change their
                                   behaviour; you only consume their output.
• config.yaml                    — routes, currency, monetization (affiliate
                                   providers + URL templates), featured_airline.
• redesign/UX_REVAMP.md          — the product concept: Answer / Watch / Lab.
• redesign/FIREBASE.md           — the Firebase architecture (data model, the
                                   per-user alert Cloud Function, security rules,
                                   cost, phased plan). Follow this closely.
• docs/redesign/answer.html      — THE PROTOTYPE. A self-contained 3-view app
                                   (Answer #/, Watch #/watch, Lab #/lab) with the
                                   final visual system, interactions, and a
                                   `FaroStore` data layer that already runs in demo
                                   mode (localStorage) and has the real Firebase
                                   modular-SDK code paths stubbed in. This is your
                                   reference implementation for markup/CSS/JS.
• redesign/REDESIGN.md, redesign/faro-reference.dc.html — the dark+gold visual
                                   identity already adopted by dashboard.py.

Run `pytest` once to see the green baseline (tests/test_dashboard.py guards the
payload/data contract — keep it green throughout).

═══════════════════════════════════════════════════════════════════════════════
GOAL
═══════════════════════════════════════════════════════════════════════════════
1. Make docs/index.html (generated by dashboard.py) BE the prototype's three-view
   app, driven by REAL data from the existing payload — not the prototype's sample
   `ROUTES`. Answer, Watch, and Lab all work against live fares.
2. Wire real Firebase (Auth + Firestore + FCM + one Cloud Function) so the Watch
   becomes a real, cross-device, alerting product — per redesign/FIREBASE.md.
3. Keep the scrape → CSV pipeline and the model exactly as they are. Firebase is an
   ADDITIVE edge, not a rewrite of the data layer.

═══════════════════════════════════════════════════════════════════════════════
HARD CONSTRAINTS — read twice
═══════════════════════════════════════════════════════════════════════════════
• DO NOT modify, move, rewrite, or delete anything under data/. It is an
  append-only open dataset of committed CSVs. Never `git rm`, never rewrite a CSV,
  never reformat it. If a migration ever seems needed, STOP and ask.
• DO NOT change scraping (provider*.py, collect.py), storage (storage.py), the model
  (predict.py), or analytics (analytics.py) behaviour. The CSVs must keep being
  written exactly as today. You may ADD a new, optional output step (write verdicts
  to Firestore) but it must be skippable and must never block or alter the CSV write.
• Firebase must be OPTIONAL and degrade gracefully. With NO Firebase config present,
  the site must still build and run exactly like today plus the new UI, with the
  Watch falling back to localStorage (the prototype's demo mode). The "$0,
  self-hostable by anyone with no accounts" promise must survive. Only when a config
  is provided do accounts/sync/alerts light up.
• Keep docs/index.html a single self-contained file (GitHub Pages serves /docs on
  main). Firebase's web SDK may load via the gstatic CDN module import (as the
  prototype does). The public Firebase web config (apiKey, projectId, etc.) is NOT a
  secret and may be injected inline; the Admin service-account JSON IS a secret and
  must only live in GitHub Actions / Firebase, never in the repo.
• Preserve the existing payload/data contract and keep tests/test_dashboard.py
  passing. Extend the payload additively if you need more fields; don't break shape.
• Preserve monetization: every "Book" CTA must keep deep-linking via the
  config.yaml provider URL templates (the prototype stubs this — make it real).
• Preserve the honest empty/collecting states and the "informational only"
  disclaimers. Trust is the product.
• Work on the current feature branch. Commit in logical steps with clear messages.
  Do NOT open a PR unless asked. Do NOT push to main.

═══════════════════════════════════════════════════════════════════════════════
DATA CONTRACT (what dashboard.py already produces — drive the UI from this)
═══════════════════════════════════════════════════════════════════════════════
The embedded `D` payload contains (verify exact shapes in dashboard.py):
  • recs    — per-route/itinerary recommendations: signal (BUY/WAIT/WATCH),
              confidence, fare now, forecast low, forecast curve + conformal band,
              expected savings, drop probability, reason text, dates, airline/stops.
  • focus   — the focused-route command-center bundle (featured carrier, etc.).
  • routes  — the list of tracked corridors.
  • stats   — dataset stats (scans, offers, routes, date_combos, airlines…).
  • (separate) docs/explore.json — the full ~1,900-itinerary finder grid.

Map these onto the prototype's components:
  • Answer  ← recs/focus for the composed trip; composer resolves a trip to the
             nearest covered itinerary in the grid; verdict = signal + best-buy
             window + confidence-with-provenance + fare-weather strip + fare story.
             Deep-linkable via ?o=&d=&dep=&len=. Pre-render the focused verdict as
             static HTML so it's readable before JS runs.
  • Lab     ← route-first: pick a route → that route's backtest accuracy, money
             saved, forecast fan, cheapest-day heatmap, and finder (its slice of
             explore.json, lazy-loaded). See "Real metrics" below.
  • Watch   ← FaroStore (Firebase or localStorage).

REAL metrics for the Lab scorecard: the prototype's accuracy/$saved are SAMPLE.
Add a real, auditable per-route backtest summary to the payload from
predict.py/analytics.py (you may extend backtest output): {calls, right, hit_rate,
saved_vs_searchday, missed_cost}. "saved" = sum over historical calls of (price you'd
have paid booking on search day − price following Faro's call). If the data is too
thin, emit nulls and have the UI show "still learning" (the prototype already
degrades this way). Never show a fabricated accuracy/savings number.

═══════════════════════════════════════════════════════════════════════════════
FIREBASE — implement for real (per redesign/FIREBASE.md)
═══════════════════════════════════════════════════════════════════════════════
Provide a real Firebase project (or document the exact setup steps if you can't
create one). Implement:
  • Auth: Anonymous on first visit (so a trip can be pinned with zero friction),
    upgradable to Google via linkWithPopup (no data migration). Promote the
    prototype's FaroStore cloud path to the real one.
  • Firestore data model exactly as in FIREBASE.md:
        routes/{route}          (public read, server-only write) — latest verdict
        users/{uid}/trips/{id}  (owner-only) — pinned trips + alerts + priceTarget
    Watch view reads users/{uid}/trips via onSnapshot (live). Verdict cards may also
    live-update from routes/{route}.
  • Security rules (firestore.rules): public read on routes/*, server-only write;
    users/* strictly owner (request.auth.uid == uid). Include the file and deploy.
  • Cloud Function (functions/) — the scheduled fan-out in FIREBASE.md: after each
    scan, read routes/* + collectionGroup('trips'), and for each user whose trip hit
    BUY / a new low / a closing window / a price target, send via their chosen
    channels. Reuse alerts.py's Telegram logic; FCM for push; Trigger-Email (or
    SMTP) for email. De-dupe with lastNotifiedSignal.
  • FCM web push (service worker + token registration) for the "Push" channel.
  • Config injection: dashboard.py reads the public web config from env/GitHub repo
    variables (e.g. FARO_FIREBASE_CONFIG as JSON) and injects
    `window.FARO_FIREBASE = {...}` inline. If unset, omit it → site stays in demo
    mode. Document required GitHub repo variables/secrets in README.
  • New scan→Firestore writer: add a step (e.g. `python -m flightwatch publish` or a
    flag on `build`) that uses the Firebase Admin SDK (service account from a GitHub
    secret) to upsert routes/{route} verdicts AFTER the CSV write and dashboard
    build. It must be a no-op when no service account is configured, and must never
    affect the CSV/commit step. Wire it into .github/workflows/daily-scan.yml as an
    optional step guarded by the secret.

═══════════════════════════════════════════════════════════════════════════════
SUGGESTED PHASING (each independently shippable; keep tests green each step)
═══════════════════════════════════════════════════════════════════════════════
1. Port the prototype's CSS/JS/markup into dashboard.py as the new shell + Answer
   view, driven by the REAL payload (replace sample ROUTES with `D`). Keep Chart.js
   or the prototype's inline SVG — your call; match the token system either way.
2. Add the Lab view (route-first gate → per-route deep-dive), driven by real recs +
   explore.json (lazy-loaded). Add the real backtest accuracy/savings to the payload.
3. Add the Watch view with FaroStore in demo (localStorage) mode — fully working
   with no Firebase yet.
4. Stand up the Firebase project; flip FaroStore to real Auth+Firestore behind the
   config; add firestore.rules, FCM, and the config injection.
5. Add the Cloud Function fan-out + the optional scan→Firestore Admin writer step in
   the workflow. Reuse alerts.py for Telegram.
6. A11y + perf pass: landmarks, focus states, prefers-reduced-motion/color-scheme,
   self-host/subset fonts, defer heavy JS, keep the Answer payload small and the
   4.3MB explore.json out of the Answer's critical path. Aim green Lighthouse on the
   Answer view.

═══════════════════════════════════════════════════════════════════════════════
ACCEPTANCE CRITERIA
═══════════════════════════════════════════════════════════════════════════════
• `python -m flightwatch build` regenerates docs/index.html as the three-view app
  from real data; `python -m flightwatch all` still scrapes and appends to data/
  CSVs unchanged; `pytest` is green.
• git history shows data/ untouched (no rewrites/deletes); a fresh clone with NO
  Firebase config builds and runs (Answer + Lab live; Watch in localStorage mode).
• With a Firebase config + service account: sign in with Google, pin a trip on one
  device and see it on another, toggle alert channels + a price target, and receive
  a real alert (push/Telegram/email) when a watched trip's verdict crosses a
  threshold. Security rules deny cross-user access.
• Book CTAs deep-link via config.yaml providers. Honest empty/collecting states and
  "informational only" disclaimers remain. No fabricated accuracy/savings numbers.

If anything here conflicts with the existing data or pipeline, STOP and ask rather
than guessing — especially anything that would touch data/ or change how/when CSVs
are written.
````
